gmail (insecure) + bash (insecure) = secure communication

> Not having the $decrypted_message from the ./receive_message comamnd stored anywhere as well
as having the eph_x25519_private.pem removed from the both sides, make the message unrecoverable

ed25519 signatures + x25519 static-static Diffe-Hellman + HKDF + AES-GCM(128)